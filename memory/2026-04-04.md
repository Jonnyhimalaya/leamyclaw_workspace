# Strategist Brief Summary — 2026-04-04

## Key Actions
1. **🔴 URGENT: Anthropic billing change enforced TODAY at 3PM ET (8PM Irish).** Claude subscriptions no longer cover third-party tools (OpenClaw). Must switch to API keys or enable "extra usage" before cutoff. One-time transition credit available (redeem by April 17). API pricing: Opus 4.6 at $5/$25 per MTok (66% cheaper than Opus 4.0). Estimated monthly API cost for our pipeline: $100-300.
2. **🔴 Kilmurry Lodge security still blocked on SSH password.** 9 CVEs in 4 days (March 18-21), CVSS 9.9 critical. Ars Technica advising "assume compromise" for unpatched instances. 40K+ exposed instances found. We're patched (2026.4.1) but Kilmurry status unknown.
3. **🟡 GA4 service account key rotation still pending.** Exposed in git history, scrubbed but key not rotated.
4. **🟢 "Managed OpenClaw" consulting package opportunity.** Multiple market signals: r/AiForSmallBusiness (28K subs), "50 Days" blog gap, community update fatigue. Proposed 3-tier pricing.
5. **🟢 Task Flow substrate (v2026.3.31) could improve our intel pipeline resilience.** Not urgent, evaluate when time permits.

## Intel Sources
- Main sweep: memory/intel-sweep-latest.md
- Scout X scan: intel-drops/x-scan.md
- Additional research: HN threads, Ars Technica, SecurityScorecard, SiliconData pricing analysis, X community discussion

## Status
- Our instance: v2026.4.1 ✅ all CVEs patched
- OpenAI credits: still need topping up (Pixel agent affected)
- Meta session: still expired
- xAI credits: ✅ topped up 2026-03-30

## Server Health: WARNING at 07:04 — mem 20% used (512MB free), disk 41% — 🟡 low RAM alert sent to Jonny via Telegram

### Heartbeat — 08:11 UTC
- **Noticed:** Low RAM warning (473MB free). Gateway using ~770MB across 2 processes. Session 3d533468 at 316 msgs (webchat, still bloated). TG session replaced.
- **Decision:** RAM not critical yet but worth monitoring. Gateway restart would reclaim memory but causes brief downtime — defer to Jonny.
- **Did:** Server health check, session check. No action needed at this level.

### Heartbeat — 09:11 UTC
- **Noticed:** Server health warning: 472MB free RAM (not critical — Linux caches aggressively). Session 3d533468 at 316 msgs (persistent heartbeat session, keeps growing).
- **Decision:** RAM is fine for now (gateway 6%+3%, SearXNG 3%, Next.js 1.4% — normal for this box). No action needed.
- **Did:** Logged. Server otherwise healthy (disk 41%, load 0.00, no Chrome zombies).

### Heartbeat — 10:11 UTC
- **Noticed:** Server health warning: 473MB free RAM (low but stable). Main consumers: gateway (6.1%), SearXNG (2.9%), Next.js, Docker. Session 3d533468 at 316 msgs (webchat, persistent bloat).
- **Decision:** RAM is tight but not critical — no intervention needed. Session bloat is a known issue, Jonny aware.
- **Did:** Logged. No escalation — Saturday, proactive mode.

## Server Health: ⚠️ WARNING at 11:04 — mem 21% used (433MB free), disk 41% — low RAM alert triggered (exit code 1). Telegram alert attempted but could not deliver from isolated session.

## Advisor Notes

### Flagged at 11:05 UTC

**1. URGENT billing deadline may be missed today.** The morning brief flags a critical Anthropic billing change enforced TODAY at 3PM ET / 8PM Irish — API key setup or "extra usage" must be enabled before this deadline. No memory entry shows Jonny was notified or that action has been taken. It is now 11:05 UTC (12:05 Irish). With ~8 hours to deadline, this needs immediate escalation to Jonny if not already handled outside memory.

**2. RAM declining with failed delivery mechanism.** RAM has dropped from 512MB → 473MB → 473MB → 433MB across four consecutive heartbeats (07:04 to 11:04). The 11:04 alert explicitly notes the Telegram delivery failed (isolated session). The memory alert system is silently broken — alerts are triggering but not reaching Jonny. Main session should attempt a direct Telegram send for this, not rely on isolated-session delivery.

**3. Session 3d533468 stuck at 316 msgs — same count for 3 checks.** Identical to yesterday's flagged issue (stuck at 256 msgs), today's session reports 316 msgs across three consecutive heartbeats (08:11, 09:11, 10:11) with no change. This is almost certainly a frozen/stale check. The session should be validated or declared frozen — passively re-logging the same number is not useful.

**4. Persistent deferred items with no progress.** Kilmurry Lodge SSH (blocked 4+ days), GA4 key rotation (exposed in git), OpenClaw v2026.4.2 update — all carried forward from yesterday with no new status. These aren't being actioned or given explicit timelines. At minimum, a consolidated ping to Jonny should be sent when he's next online.

### 🚨 CRITICAL: Anthropic Billing Change — 11:11 UTC
- **Noticed:** Confirmed via X search: Anthropic is enforcing billing change TODAY (April 4). Claude subscriptions no longer cover third-party tools (OpenClaw). Rolling out ~12pm PT / 8PM Irish.
- **Decision:** URGENT — our auth profiles `anthropic:claw` and `work` both use `mode: "token"` (subscription). Must switch to API keys before cutoff.
- **Did:** Verified auth config. Both Anthropic profiles are subscription tokens. Need Jonny to:
  1. Go to console.anthropic.com → API Keys → create a new API key
  2. Claim the transition credit (Settings > Usage, available until April 17)
  3. We'll update `auth-profiles` to `mode: "api_key"` with the new key
- **Impact if missed:** ALL Anthropic models stop working (Opus, Sonnet) — only fallbacks (Gemini, GPT, OpenRouter) remain
- **Workaround:** OpenRouter profile still works as it's API-key based, but at higher cost
## Server Health: OK at 15:04 — mem 25%, disk 43%

### Nexus Agent Deployment — 14:52-16:50 UTC
- **Noticed:** Jonny wants a separate OpenClaw for his BTC company (Nexus), as a POC for the Kilmurry multi-tenant pattern
- **Decision:** Full deployment following same architecture as Kate plan — separate OS user, separate gateway, shared LLM keys
- **Did:**
  1. ✅ Created `nexus` OS user
  2. ✅ Installed OpenClaw 2026.4.2 + Claude Code 2.1.92
  3. ✅ Gateway on port 18790 (systemd, auto-restart, boot-persistent)
  4. ✅ Telegram bot @OnNexus_bot connected + paired
  5. ✅ Auth failover (profile rotation + cooldowns)
  6. ✅ QMD memory (Gemini embeddings — free, no OpenAI dependency)
  7. ✅ Security: 100/100 compliance, heal.sh fixes, file permissions
  8. ✅ Workspace bootstrapped (SOUL.md, AGENTS.md, USER.md, MEMORY.md)

### Deployment Gap Analysis + Protocol Fix — 16:00-16:12 UTC
- **Noticed:** Jonny caught that I missed health.sh during Nexus deployment. ~50% of optimisations were missed because I worked from memory instead of following the playbook.
- **Decision:** Structural fix, not just "try harder"
- **Did:**
  1. Added mandatory "Client Deployments" rule to AGENTS.md — forces playbook-first approach
  2. Created deployment-checklist.md for Nexus as proof artifact
  3. Logged correction in .learnings/corrections.md
  4. Closed all gaps: health.sh, SearXNG, HEARTBEAT.md, vault structure, protocols C/D, autonomy levels, channel trust, git init, crons
  5. Final score: 95% (1 deferred — advisor cron)
- **Key lesson:** Checklists > memory. Always.

### Nexus Mission Control Built — 16:48-17:00 UTC
- **Noticed:** Nexus agent provided detailed module wishlist including Vegas War Room, Capital Pipeline, Ecosystem Intelligence, Pitch Ammo
- **Decision:** Build via Claude Code (OAuth = free) rather than API-heavy manual build
- **Did:**
  1. Wrote 130-line spec (/tmp/nexus-mc-spec.md)
  2. Spawned Claude Code via ACP — built full 8-page Next.js app in ~2 minutes
  3. Pages: Landing, Vegas War Room, Capital Pipeline, Ecosystem, Data Room, Pitch Ammo, Agent Health, Documents
  4. PM2 deployed on port 3334
  5. Live at http://172.239.114.188:3334
- **Client tracked at:** consultancy/clients/nexus/

### Mission Control — Updated for Nexus client
- Added Nexus to consultancy clients page alongside Kilmurry Lodge
- Built and PM2-restarted Leamy Mission Control

### Auth Verification — 12:31-12:38 UTC
- **Noticed:** Jonny worried about Anthropic OAuth cutoff at 8PM Irish
- **Did:** Checked auth profiles — both `anthropic:claw` and `work` show as `static` tokens starting with `sk-ant-o...` (API keys, not OAuth). Cutoff won't affect us. Jonny disagreed — will verify when deadline passes.

### Pending for Jonny
- `/new` this session (569 messages, critical bloat)
- Verify Anthropic auth after 8PM Irish cutoff
- GA4 key rotation (still open)
- OpenAI quota top-up (Content agent still dead)
