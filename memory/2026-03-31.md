# 2026-03-31

## Scout Cron Fix (~08:10-08:19 UTC)
- All 3 Scout cron jobs (morning, evening, intel sweep) were failing with `LiveSessionModelSwitchError`
- Root cause: Scout agent uses xai/grok-3-mini but global default is claude-opus-4-6 — cron can't handle the mismatch even in isolated sessions
- Known bug, supposedly fixed in changelog but still broken on 2026.3.28
- Fix: moved intel sweep to main agent with Sonnet model — runs and delivers successfully
- Removed all 3 broken Scout crons, kept working main-agent intel sweep
- Active crons now: Server Health (4h), Track Costs (4h), Dream Cycle (2am), Intel Sweep (6am)
- ⚠️ Telegram session hit 439 messages — told Jonny to /new

## Heartbeat 00:28 UTC
- Server health: OK (14% mem, 1070MB free, no Chrome, disk 38%, load 0.01)
- ⚠️ Session `bf1229ff` at 363 messages — critically large
- Created daily memory file

## Server Health: OK at 03:04 — mem 15%, disk 38%

## Server Health: OK at 07:04 — mem 15%, disk 38%

## Telegram Session (~08:22-10:33 UTC)

### Every.to Research
- Researched every.to — Dan Shipper's media + AI products company
- Products: Spiral (content repurposing), Cora (email), Sparkle (file org), Lex (writing)
- Launching "Plus One" — OpenClaw-powered AI agents for subscribers
- Validates Jonny's consultancy model: bespoke > generic, same playbook different audience

### 529 Overloaded Debug — ROOT CAUSE FOUND
- Jonny's tweet link caused 30+ duplicate deliveries, 14-minute delay
- Root cause: Anthropic API returning 529 (overloaded) from 09:03-09:17
- OpenClaw kept retrying same model — known bug (#49079, #34535, #26578, #49696)
- HTTP 529 doesn't trigger fallback chain — error classified as terminal instead of failover-eligible
- Bug is in embedded run streaming path, bypasses `classifyFailoverReason()` which correctly identifies overloaded
- `retry` config only exists for cron jobs, NOT live sessions — wasted time trying to add it to agents config
- ⚠️ Broke config twice: `agents.entries` (doesn't exist), `agents.defaults.retry` (doesn't exist), `agents.list[].retry` (doesn't exist). Doctor fixed each time.

### Fallback Chain — CONFIGURED ✅
- Added Google Gemini (gemini-api-key via `openclaw onboard`)
- Added OpenRouter (API key added directly to auth-profiles.json)
- ⚠️ Onboard wizard changed primary model to Gemini — had to revert to Opus
- Final chain (6 deep):
  1. Opus (Anthropic direct) — primary
  2. Opus via OpenRouter — same model, different route
  3. Sonnet (Anthropic direct) — lighter model
  4. Gemini 3.1 Pro — separate provider entirely
  5. GPT-5.4 — needs OpenAI credits topped up
  6. GPT-4o — needs OpenAI credits topped up
- Gateway restarted, config clean, no errors

### Model Failover Transparency Rule — Added to AGENTS.md
- Standing rule: if running on fallback model, tell Jonny in first response
- Known 529 bug documented in AGENTS.md

### Lessons Learned
- ALWAYS check `--help` and docs BEFORE giving commands to Jonny
- ALWAYS validate config schema location before writing to openclaw.json
- `openclaw onboard` interactive wizard changes primary model — warn user
- Don't trust X search for CLI commands — often AI-generated garbage
- OpenRouter key was shared in chat — told Jonny to delete message

## Intel Sweep — 08:12 UTC (Cron)
Ran comprehensive OpenClaw ecosystem intelligence sweep. Key findings:
- **v2026.3.22-3.29 release wave**: 45+ features, 13 breaking changes, 82 bug fixes. Breaking: Qwen OAuth removed, old config migrations dropped, browser control system overhauled.
- **ClawHub marketplace live**: 13,700+ skills. 10.8% malicious plugin rate in earlier audits — ClawNet security scanner recommended.
- **Plugin approval hooks**: New `requireApproval` in before_tool_call — enables human-in-the-loop for enterprise clients.
- **Community trend**: "Strip skills, use Sonnet" performance fix going viral — skill bloat confirmed as major perf killer.
- **Cost optimization**: Azure via LiteLLM ($1000 free) gaining traction as budget play.
- **awesome-openclaw-usecases**: 42 curated use cases including CRM, content factories, game dev pipelines — good pitch material.
- **250K+ GitHub stars**, NVIDIA NemoClaw stack, OpenClaw Foundation transition underway.
- Full report: `memory/intel-sweep-latest.md`

## Telegram Session (~10:50-13:40 UTC)

### Mission Control — Research Bank Detail Pages ✅
- Built `/consultancy/research/[slug]` dynamic route (same pattern as playbooks)
- Research cards now clickable, full articles render with tables, code blocks, blockquotes
- All 6 research documents browsable at localhost:3333/consultancy/research

### Forminator Check (Last 5 Days)
- 4 submissions on "Contact Leamy Maths" form (Mar 26-31):
  - #837 Jack O'Sullivan — booking 6 revision classes (Circle, Diff, Integration, Fin Maths, Geometry, Binomial)
  - #836 Isobel Stack — pay-as-you-go, partial attendance
  - #835 Anne O'Keeffe — booking Jack Downes for Sunday revision bundle
  - #834 Stephanie McNamara — son at Ard Scoil Rís, OL Maths, wants grinds (referral)
- Updated revision-course-2026.json with Jack O'Sullivan's 6 sessions

### Consultancy Playbooks Written
1. **OpenClaw Server Audit Protocol** — 8-phase SSH audit checklist (infrastructure, config, memory, security, cost). Based on Kilmurry experience. Includes one-liner quick-check script.
2. **Security Hardening Playbook** (Draft v1) — 6-layer security model for client deployments handling sensitive data:
   - Layer 1: Network (Tailscale, UFW, SSH hardening)
   - Layer 2: Channel lockdown (allowFrom, dmPolicy)
   - Layer 3: Data segregation (vault structure, MEMORY.md rules)
   - Layer 4: Agent sandboxing (workspaceOnly, exec deny/allowlist)
   - Layer 5: Credential management (.env, rotation schedule)
   - Layer 6: Monitoring (security audit cron, anomaly detection)
   - Includes implementation checklist and client communication template
- Both synced to workspace-consultancy, Mission Control rebuilt

### Key Findings — Security
- Our own Leamy setup has NO allowFrom on Telegram — anyone can message the bot
- Kilmurry same — no allowFrom, no channel lockdown
- `openclaw security audit --deep` shows 3 warnings on our server (multi-user heuristic, weak fallback model, no trusted proxies)
- Mission Control has zero native auth — network-level protection only option
- Community reports 63% of OpenClaw instances fail basic security checks

## 08:48 — Intel Sweep Received
- OpenClaw v2026.3.22+ has 13 breaking changes — need to audit configs before updating (us + Kilmurry)
- ClawHub marketplace live (13,700+ skills) but 10.8% malicious plugin rate — consider ClawNet for client deployments
- New plugin approval hooks for human-in-the-loop workflows — relevant for consultancy pitch
- Community validating Opus+Sonnet multi-model pattern (our architecture ✅)
- "Strip skills, use Sonnet" perf fix trending — worth auditing our skill bloat
- Full report: memory/intel-sweep-latest.md
## Server Health: OK at 11:04 — mem 18%, disk 38%
## Server Health: OK at 15:04 — mem 18%, disk 38%
