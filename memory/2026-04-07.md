# Memory — 2026-04-07

## Strategist Brief Summary
Five items in tonight's action brief (evening run, 19:58 UTC):

1. **🔴 Anthropic credit claim deadline April 17** — 10 days remaining. One-time credit equal to monthly plan fee. Must claim at console.anthropic.com → Settings → Usage. Also: create API key and migrate from OAuth tokens while there. Estimated post-migration Anthropic cost: $50-100/mo with GPT-5.4 as primary.

2. **🔴 Upgrade to v2026.4.5** — Fixes pairing-required regression, sessions_spawn scope-upgrade bug, and exec-approvals corruption. Run `openclaw doctor --fix` (2-3x) before upgrading. Community warns about potential `dmPolicy` invalid key insertion — check openclaw.json manually. Main VPS on v2026.4.1, Kilmurry on v2026.4.2.

3. **🔴 RAM critical on main VPS** — 259MB free, swap in use. Immediate fix: clear Nexus npm caches (9.3GB), stop Nexus gateway when idle, add 2GB swap file. Medium-term: upgrade to 16GB VPS (~€15/mo Hetzner).

4. **🟡 GA4 key rotation** — 4 days unresolved. Blocks Kate's dashboard. Target: fix before Wednesday Kate meeting.

5. **🟢 ClawHub supply chain audit** — 12%+ malicious rate. Audit installed skills before v2026.4.5 upgrade (new ClawHub UI makes installs easier). Restrict agent skill-install permissions in configs.

## Kate TG Bot + Whisper Setup — Session 1 (15:14-16:05 UTC, lost to /new)
_Recovered from Jonny's paste of earlier session. Protocol C failure — none of this was written to memory before /new._

### What was done:
1. **TG bot created and configured** — @Katetaylor123_bot, token: 8601857637:AAFtkBfTuXMhnF8TlwUjdLqQ6QsOda05nHo
2. **Kate paired** — TG ID 8778805348, username katetaylor123, approved via `openclaw pairing approve telegram QBCY5BVQ`
3. **Mission Control status reviewed** — 9 pages all 200, live data on competitors/events/reviews/SEO/AI log. Still placeholder: analytics (GA4), campaigns (Meta), email, content.
4. **MC access** — localhost:3334 on Kilmurry server. No public URL yet.
5. **Whisper installed** — hit disk full (100%), cleared 4GB of pip cache, got back to 4.2GB free
6. **exec-approvals.json fixed twice:**
   - First: invalid JSON (no quotes around keys), whisper not in allowlist → fixed JSON, added whisper + ffmpeg
   - Second: file had `chattr +i` (immutable from Apr 4 hardening) → removed immutable flag
7. **Tailscale** — server already on Jack's tailnet at 100.111.174.13. Kate needs to:
   - Download Tailscale on laptop
   - Jack invites her via Tailscale admin (login.tailscale.com/admin/users or app → Users → Invite)
   - Once joined: Kate's MC at http://100.111.174.13:3334, Jack's at http://100.111.174.13:3333
8. **Kate still got no response to voice message** at 15:51 — agent still couldn't run whisper due to EPERM on exec-approvals.json

### Credentials confirmed:
- Kate's user password: KilmurryKate2026!
- Jack's server password: Castletroy1995!
- Kilmurry Tailscale IP: 100.111.174.13

### Still needed from Kate (Wednesday meeting):
- GA4 property access (add service account as viewer)
- Meta/Facebook API token
- Email platform choice + API key
- Google Ads account ID

## Kate Voice Message Fix — Session 2 (20:40-20:52 UTC)
- **Noticed:** Jonny reported Kate's voice message still not working after server upgrade.
- **Root cause:** Same `chattr +i` issue on exec-approvals.json. Earlier session fixed it but the fix didn't fully persist (or gateway restart re-applied state).
- **Did:**
  1. SSH'd as kateuser — confirmed exec-approvals.json has correct JSON with whisper + ffmpeg in allowlist
  2. SSH'd as clawuser with sudo — `chattr -i` on exec-approvals.json
  3. Verified SOUL.md + AGENTS.md still immutable (correct)
  4. Restarted openclaw-kate.service — confirmed running
- **Status:** Should be working now. Kate needs to test.
- **⚠️ Learning:** Never `chattr +i` a file the gateway needs to READ at runtime. Immutable flag blocks ALL access. Only lock truly static files (SOUL.md, AGENTS.md).

## Mission Control — Blocked by Jonny Panel — 20:16 UTC
- **Did:** Built and deployed "Blocked by Jonny" panel on main dashboard
- Items: GA4 key rotation, Kilmurry SSH password, Meta re-login, credits top-up
- Each item shows: impact, what's blocked, next action, due date, owner status
- Committed: 5662555 in mission-control repo
- API currently hardcoded — future: wire to human-tasks.json or dedicated blocked-items file

## GPT-5.4 Switch — 20:13 UTC
- Jonny manually switched main agent to GPT-5.4 via /model
- Tested it on Mission Control panel build — worked well, clean code, built first try
- Switched back to Opus for deeper diagnostic work (Kate voice message debugging)
- GPT-5.4 is viable as daily driver. Opus for complex/multi-step work.

## Reliability / Memory Audit — 21:00-21:48 UTC
- **Noticed:** Jonny flagged serious forgetfulness and versioning drift. Main failure points: not remembering prior GitHub token/setup context, not pushing changes reliably, and losing key session work across `/new`.
- **Decision:** Treat as a real operational regression, not user confusion. Audit git history, memory history, AGENTS changes, and backup compliance.
- **Did:**
  1. Confirmed git backup protocol existed in AGENTS.md and I had been out of compliance
  2. Committed + pushed both repos for rollback safety:
     - Workspace: `7618503` — Update memory, Kate deployment records, and learnings
     - Config: `fe02d52` — Update OpenClaw config and cron state
  3. Reviewed last 2 weeks of workspace/config history to identify likely regression commits
  4. Diagnosed likely over-optimisation: AGENTS trimmed too aggressively, more architecture added, less redundancy retained
  5. Restored missing guardrails into AGENTS.md and pushed:
     - `c072809` — Restore AGENTS memory and versioning guardrails
- **Conclusion:** Not a full rollback. Selective restoration of lost memory/versioning/debugging scaffolding while keeping newer architecture.

## Kate / Jack GitHub Issues — Status for Tomorrow
- Kate dashboard local repo restored on server
- Current local commit on Kate dashboard: `5c11f32` — Restore repo and add competitor review tabs
- Push to GitHub still unresolved
- Findings:
  - Repo recorded in docs/memory as `Jonnyhimalaya/Kilmurry-Kate`
  - `.git` folder had been missing from Kate dashboard folder, so local repo state was lost
  - Neither `kateuser` nor `clawuser` currently has working GitHub auth on Kilmurry server
  - HTTPS fine-grained token flow likely the right fix next
- Need to continue tomorrow on:
  1. Kate dashboard push/auth fix
  2. Jack/Kate GitHub setup audit
  3. Verify Kate voice note works after exec-approvals fix

## Key Research Findings
- Anthropic migration confirmed straightforward (~20 min), no widespread failures
- Community using `--max-memory=4096` flag per agent to cap RAM
- `openclaw doctor --fix` may need multiple runs; can introduce stale keys
- v2026.4.5 adds video/music generation, 12-language UI, ACPX embedded runtime
- Community rapidly moving to local models (Ollama) and provider diversification post-Anthropic billing change
