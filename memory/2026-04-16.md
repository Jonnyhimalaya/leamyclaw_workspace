# 2026-04-16 — Strategist Brief Summary

## Morning Action Brief Delivered
5 items, 3 critical:

1. **🔴 Jack's ACP harness broken** — Claude Max subscription no longer covers third-party harnesses since April 4. Jack's Claude Code ACP setup for Kilmurry is either failing or burning extra usage credits unknowingly. One-time credit ($200) expires April 17 (tomorrow). Recommended: contact Jack today, migrate to API key billing or extra usage bundles, consider Codex provider as alternative.

2. **🔴 Upgrade servers v2026.4.12 → v2026.4.14** — 5 security fixes including realpath file restriction bypass and config.patch flag hardening. Both main VPS and Kilmurry need upgrading.

3. **🔴 GA4 key rotation 13 days overdue** — Still exposed in git history, still blocking Kate's dashboard. Needs escalation approach change.

4. **🟡 OpenAI + xAI credits empty** — Scout, Pixel, and hybrid search all degraded. Pipeline feeding the morning brief is impaired.

5. **🟢 Consultancy opportunity** — Anthropic billing migration is the hottest topic in OpenClaw community. Document our Jack migration as a case study, write a migration guide, offer billing migration audits as a service.

## Key Research Findings
- Community workarounds for Anthropic change: Cowork bridge (sub-safe), Dario proxy (OAuth, risky), API key migration (recommended by OpenClaw team), provider switching (Codex provider bundles managed auth).
- v2026.4.14 confirmed stable by community, no breakage reports.
- OpenClaw ecosystem at peak mainstream attention (Lenny's Newsletter, DataCamp, multiple tutorial sites).

## Pattern Noted
Four action items blocked on Jonny taking small admin actions. Recommended batching into one 15-minute block.
## Server Health: WARN at 07:04 — mem 49%, swap 440MB, disk 58% — Jonny notified via Telegram
## Server Health: WARN at 11:04 — swap 438MB, mem 50%, disk 58% — alert sent to Jonny

### Heartbeat — 11:39 UTC (Swap Investigation)
- **Noticed:** Swap at 438MB consistently for days, advisor flagged it as uninvestigated
- **Decision:** Investigated root cause rather than just repeating alert
- **Did:** Found root cause — nexus gateway using 2GB RAM (24.6%, PID 449383, running since Apr 4 = 12 days). For comparison, jonny gateway is using 1GB. Both gateways + SearXNG + two Next.js servers = ~3.5GB active RAM on an 8GB box. Swap is the overflow.
- **Fix options for Jonny:**
  1. Stop nexus gateway when not actively testing it (`sudo -u nexus openclaw gateway stop`)
  2. Upgrade VPS to 16GB RAM
  3. Schedule nexus gateway for automatic restart daily (clears memory leaks)

## [16:15 UTC] Browser Automation Session - WC Memberships

**Noticed:** OpenClaw v2026.4.12 upgrade broke browser plugin proxy routing (CDP issues). Headless Playwright still works but requires `['--no-sandbox', '--disable-gpu']` args and JS force-click for hidden buttons.

**Decision:** Used raw Playwright scripts (not OpenClaw browser tool) to add WC memberships.

**Did:**
- Identified 16 students on plan 8858 (5 HL EASTER 2022) who joined in last 6 months
- Jonny had already done these manually (wasted effort — should have confirmed first)
- Still confirmed the automation approach works for future use

### Key Lessons from This Session

**WC Memberships automation quirks:**
- `#publish` button is hidden by WP metabox overlays — use `page.evaluate(() => document.querySelector('#publish').click())` not `page.click('#publish')`
- First save always lands on `post_status=publish` (broken) — must save TWICE to get `wcm-active`
- Member dates are in `.member-since-date` span, user ID is in `.post_author` div (NOT in href params)
- Plan 8858 = "5 HL EASTER 2022", Plan 6076 = "6th Year HL Returning Term 1"

**Browser tool broken since v2026.4.12:**
- OpenClaw browser tool fails with CDP/proxy routing errors
- Fix applied: `browser.enabled=true, headless=true, noSandbox=true` in config
- But tool still not working via OpenClaw — raw Playwright is the workaround until v2026.4.14 upgrade
- **Action needed: Upgrade to v2026.4.14** (fixes Kate's Telegram alerts AND CDP issues)

### Upgrade OpenClaw to v2026.4.14
- Both servers still on v2026.4.12
- v2026.4.14 specifically fixes CDP browser proxy issues
- Run: `npm install -g openclaw@latest && openclaw gateway restart`

## Server Health: ISSUES at 19:04 — mem 44%, disk 59%, swap 493MB

## Advisor Notes
- **Process / Verification:** The 16:15 UTC browser automation session resulted in wasted effort because the agent did not verify the target state (whether students were already added) before executing the script. Always verify current state before taking action.
- **Security (Browser):** Setting `noSandbox=true` for the browser configuration is a significant security risk. Headless browser sandboxing should remain enabled unless absolutely running in an isolated container that forbids it.
- **Security (Credentials):** The GA4 key is noted as being exposed in git history and 13 days overdue for rotation. This is a critical credential leak that requires immediate remediation (rotation and git history scrubbing), but no active steps were taken to resolve it today.
