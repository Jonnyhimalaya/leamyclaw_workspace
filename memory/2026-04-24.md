# Memory Log — 2026-04-24

### Heartbeat — 08:39 UTC
- **Noticed:** Session 3d533468 at 1323 messages. Swap at 493MB. Today's memory file was missing. Chrome had accumulated 4 idle processes (~1GB).
- **Decision:** Create today's memory file and escalate session bloat + swap. Chrome cleanup already handled by health script.
- **Did:** Created memory file. Flagging to Jonny.
# Memory Log — 2026-04-24

### Heartbeat — 08:39 UTC
- **Noticed:** Session 3d533468 at 1323 messages. Swap at 493MB. Today's memory file was missing. Chrome had accumulated 4 idle processes (~1GB).
- **Decision:** Create today's memory file and escalate session bloat + swap. Chrome cleanup already handled by health script.
- **Did:** Created memory file. Flagging to Jonny.

---

### Aidan Deployment — Completed 2026-04-22 (logged here for durability)
- **Server:** Linode 172.237.101.137, Ubuntu 24.04.3 LTS, 4GB, clawuser
- **Access:** SSH key-only (ed25519 `jonny-main-vps-to-aidan`), password auth disabled, clawuser password rotated post-hardening
- **Lockout recovery:** Aidan opened Lish console, reset root pw, reset clawuser to Secure2026 (temp), I re-entered via pw, installed key, verified fresh session with key, THEN hardened SSH. Proper order this time.
- **OpenClaw:** v2026.4.21, Node v24.15.0 via nvm
- **Auth:** Claude Code OAuth (via `claude` CLI, logged in as aidan@the8life.com, Claude Pro plan) — reused by OpenClaw via `claude-cli` provider
- **Models:** Primary `claude-cli/claude-opus-4-7`, fallback `claude-cli/claude-sonnet-4-6`
- **Why OAuth not API:** Aidan's API funding is separate wallet from OAuth funding. He funded OAuth. Going OAuth avoided double-funding.
- **Gateway:** systemd service `openclaw-gateway` active, port 19000, `gateway.mode=local`
- **Telegram bot:** @Aidanobot (DigiAidan) token `8505164797:AAGdhKSBzyW8xCPW7jSvSPJCg-TTEmoO-Zg`
- **Whitelist:** Aidan's Telegram ID `7462203606` paired + approved
- **Agent name:** Atlas 🎯 (carries operational load so Aidan stays in highest-value mode)
- **Workspace:** Full SOUL/USER/IDENTITY/AGENTS/MEMORY/TOOLS tuned to BTM premium advisor voice
- **Vault seeds:** business/btm-core.md, business/pilot-30d.md, priority-map.md (Tier 1-4 ranked)
- **Exec approvals:** main=full, defaults=allowlist+ask
- **Health script:** scripts/server-health.sh deployed, cron configured silent-unless-alert
- **.learnings:** Seeded ERRORS.md with SSH lockout lesson (access preservation gate, mandatory sequence)
- **Usage status (2026-04-24):** Gateway up, Aidan paired, but **barely/no real usage since setup**. Session exists but only test-era activity.

---

### Main VPS — Cron Noise Issue (2026-04-23 → 2026-04-24)

**Problem surfaced:** "Server Health Check" cron sending FallbackSummaryError walls to Telegram — all 5 models failing.

**Root cause:** Cron was model-driven (invoked assistant, not just shell script). Model chain:
- primary: openai-codex/gpt-5.4
- fallbacks: anthropic sonnet-4-6, gemini-3.1-pro-preview, anthropic opus-4-7, anthropic opus-4-6, openrouter opus-4-6

All 5 doors shut simultaneously:
- Anthropic: OAuth blocked for org + profiles in cooldown
- Google: rate-limited / monthly spend cap exceeded
- OpenRouter: 402 insufficient credits

**Fix applied 2026-04-23:** Rewrote Server Health Check cron as direct shell script (no model invocation). Raised swap thresholds (warn >500MB, critical >800MB) to stop noise. Tested manually — silent unless issue, correct.

**Related fix:** Set Anthropic auth order override to prefer `work` profile, then `anthropic:claw`. Later reverted to `anthropic:claw` → `work` on 2026-04-24 when Jonny said work account was exhausted, then flipped again.

**Second failure surfaced 2026-04-24:** "Advisor Check" cron still failing — because IT is still model-driven (different cron from Server Health Check). Only Server Health Check was de-LLM'd. Advisor Check legitimately needs model judgment, so can't just strip it.

**Durable learning:** Health/telemetry crons should be model-free. Only LLM-crons should invoke models. Need systematic audit of all crons.

---

### Live-session vs Fresh-run Anthropic Paradox (2026-04-24)

**Observation:** Live Telegram chat works on work Anthropic token. Fresh cron runs get `auth_permanent` and skip Anthropic entirely.

**Root cause analysis:**
- Live session resolved auth when healthy, cached the lane, still riding warmed profile
- Fresh runs re-resolve from scratch every time, hit current degraded state
- OpenClaw's `auth_permanent` flag = "provider refused me, don't know when it'll recover"
- Cooldown state persists on the profile even after the actual provider issue clears

**Likely underlying causes (ranked):**
1. Anthropic rate-limited or restricted the OAuth token
2. Cooldown state stuck on profile (OpenClaw saw one real error earlier, keeps skipping)
3. Org-level issue on Anthropic account (literal 403: "OAuth authentication is currently not allowed for this organization")
4. Live chat lane doesn't re-auth mid-session — never learns state changed

**Fix path (not yet executed):**
1. Clear Anthropic auth cooldown state
2. Refresh Anthropic token / OAuth login
3. Re-run isolated Anthropic test
4. If still failing → Anthropic-side org/billing/restriction issue

**Durable rule:** Live-session success ≠ provider health. Fresh-run tests are the real signal.

---

### Context Bleed Issue (2026-04-24, CRITICAL)

**Pattern observed:** I kept mentioning "Aidan" while discussing Dano's setup. Kept pattern-matching against last similar deployment instead of forcing fresh lookup per client.

**Root cause:** Too many client deployments held in one live session. No canonical access registry. Relying on memory/chat context instead of structured per-client lookup.

**Jonny's direct feedback:** "How are we gonna run a consultancy if you keep losing access?" + "why are all of these context bleeds happening? We need to fix the issue."

**Agreed permanent fix (not yet executed):**
1. Create `consultancy/client-access-registry.md` — canonical, one row per client:
   - Client, Server IP, SSH user, Access method, Recovery path, Bot, MC URL, Last verified, Status, Notes
2. One per-client ops sheet per client
3. Hard rule in AGENTS.md: **no client advice, no commands, no access claims unless I first read that client's row**
4. Weekly access audit cron checking every client box
5. No deployment marked complete until re-access is tested from scratch
6. Password-only access treated as incomplete

**Lesson for .learnings/:** Client context must be explicitly loaded per task, not inferred from chat history. Pattern-matching across clients is lethal for consultancy work.

---

### Dano Access Lost (2026-04-24)

**Observation:** Stored Dano SSH password no longer works. Could not verify his deployment status.

**This is exactly what Jonny got frustrated about** — losing access between sessions. Reinforces the need for the access registry.

**Blocker:** Aidan (wait — no, DANO. See? Context bleed again.) — Dano SSH password auth failing. Need:
- Linode Lish console access, OR
- Jonny to push a fresh SSH key via Linode console as root

**Once recovered:** Must add Dano to access registry with verified key auth + backup recovery path documented.

---

### Jonny's Two Anthropic Accounts

- **Personal** (exhausted mid-session 2026-04-24)
- **Work** (currently the one live chat is riding)

Flip-flopped auth order twice in one session based on which was working. Durable fix = top up one, not juggle.

---

### GPT-5.5 Released (2026-04-23)

Jonny asked how it compares to Opus 4.7 for agentic/proactive work.

**My read:**
- Not enough OpenClaw community signal yet (released day before)
- GPT-5.5 probably competitive for orchestration, tool-calling, routing
- Opus 4.7 still safer for proactive judgment, long-horizon behavior, "sharp operator" feel
- Recommended: Opus 4.7 for main strategic, GPT-5.5 worth hard-testing for orchestrator role
- Proper bake-off needed: same tasks, same tools, score initiative/judgment/output quality

---

### Session Cache Observation

Current session hit ~1400+ messages at flush time. Aidan deployment + crons + Dano recovery + model questions all crammed into one session. Contributed directly to context bleed problem.

**Durable rule:** Heavy multi-client ops work should be split across sessions or explicitly scoped. /new after major client deployments.

---

## Advisor Notes — 2026-04-24 19:01 UTC

**Two committed fixes still unexecuted:**

1. **`consultancy/client-access-registry.md` not created** — Jonny explicitly flagged context bleed as a consultancy-critical problem. The agreed fix (canonical per-client access table + hard lookup rule in AGENTS.md) was documented but not actioned. Dano access is currently lost. This file needs to exist before the next client interaction.

2. **Anthropic auth cooldown not cleared** — Fix path identified (clear cooldown state, refresh OAuth token, run isolated test) but marked "not yet executed". Advisor Check cron is still model-driven and failing fresh runs while live chat works. This is a misleading operational state.

**Dano access still broken** — No resolution path confirmed. Needs Linode Lish or Jonny-pushed key. Not just documented — still an open blocker.
